<%= form_for(@account) do |f| %>
  <% if @account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% @account.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
     <%= f.label :name %><br />
     <%= f.text_field :name %>
   </div>
   <div class="field">
     <%= f.label :budget %><br />
     <%= f.text_field :budget %>
   </div>
    <div class="field">
      <%= f.label :cash %><br />
      <%= f.number_field :cash %>
    </div>
    <div class="field">
      <%= f.label :user %><br />
    </div>

    <div class="field">
      <%= f.label :split_account %><br />
      <%= collection_select :account, :split_account_id, current_user.accounts, :id, :name, :prompt => true %>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
